# AGENTS.md

## 执行回报规则（强制）

当代理进入“连续执行”状态时，必须在每次任务执行完成后主动回报结果，不得等待用户追问。

每次回报至少包含以下 5 项：
1. 执行了什么命令或动作
2. 关键结果（通过/失败、数量、错误摘要）
3. 产生或更新了哪些文件
4. 是否已提交（如有，给出 commit hash）
5. 下一步将自动执行什么

如果执行失败，必须在同一条回报中给出：
- 失败原因
- 已尝试的修复动作
- 下一步补救方案
